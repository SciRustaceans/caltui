# Build output
/bin/
/dist/

# Secrets / local env (never commit API keys)
.env
*.env
!.env.example

# Local data / scratch
*.db
*.db-wal
*.db-shm
/tmp/
/.scratch/

# ETL working files (raw USDA CSV downloads & intermediate db)
/tools/etl/_data/
/tools/etl/*.sqlite
/tools/etl/*.zip
# ETL build temp (the committed asset is internal/seed/foods.sqlite.gz)
internal/seed/*.build.sqlite*

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code internal state
/.claude/
